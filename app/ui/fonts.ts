import { Inter } from "next/font/google";
import { Noto_Sans_JP } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const notojp = Noto_Sans_JP({ subsets: ["latin"] });
